jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, CircleCI.
      - run:
          name: Print the Current Time
          command: date