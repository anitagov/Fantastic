jobs:
  build:
    docker:
      - image: eclipse/ubuntu_jdk8
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, CircleCI.
      - run:
          name: Print the Current Time
          command: date
